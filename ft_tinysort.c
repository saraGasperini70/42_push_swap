#include "includes/push_swap.h"

void    ft_tinysort(t_stack **stack)
{
    int highest;

    if (ft_sorted(*stack))
        return ;
    else if (stack->next->index == highest)
        ft_sa(stack);
}